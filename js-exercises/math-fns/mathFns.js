function sqrt(sqrtNumber) {
  return Math.sqrt(sqrtNumber);
}
function power(base, exponent) {
  return Math.pow(base, exponent);
}
function round(roundNum) {
  return Math.round(roundNum);
}
export { sqrt, power, round };
